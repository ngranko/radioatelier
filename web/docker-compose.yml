services:
    app:
        build:
            context: .
            dockerfile: docker/node/Dockerfile
            target: prod
        ports:
            - 3000:3000
        restart: unless-stopped
