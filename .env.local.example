# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=dev:[your-deployment-id]

# Frontend (SvelteKit)
PUBLIC_CONVEX_URL=https://[your-deployment-name].convex.cloud
PUBLIC_CONVEX_SITE_URL=https://[your-deployment-name].convex.site
PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxx

# Clerk (SvelteKit server-side)
CLERK_SECRET_KEY=sk_test_xxxxxxx

# Typesense (local scripts — sync/search keys go in Convex dashboard)
TYPESENSE_URL=https://[your-deployment-name].typesense.cloud
TYPESENSE_ADMIN_KEY=[your-admin-key]
TYPESENSE_SEARCH_KEY=[your-search-key]
TYPESENSE_SYNC_KEY=[your-sync-key]
TYPESENSE_BACKFILL_KEY=[your-backfill-secret]
TYPESENSE_COLLECTION=objects

# Convex deployment env vars (set via `npx convex env set`, not in .env.local):
#   CLERK_JWT_ISSUER_DOMAIN  — Clerk JWT issuer for Convex auth
#   CLERK_WEBHOOK_SECRET     — Svix signing secret for user sync webhook
#   GOOGLE_API_KEY           — Server-side geocoding and Places search
#   TYPESENSE_URL, TYPESENSE_SYNC_KEY, TYPESENSE_SEARCH_KEY, TYPESENSE_BACKFILL_KEY

# Production (adapter-node):
#   ORIGIN=https://your-domain.com
